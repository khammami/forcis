% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-server.R
\name{forcis_mcp_server}
\alias{forcis_mcp_server}
\title{Launch a FORCIS MCP server}
\usage{
forcis_mcp_server(...)
}
\arguments{
\item{...}{Additional arguments passed to \code{mcptools::mcp_server()}.}
}
\value{
This function does not return. It blocks the current R process to
serve MCP requests.
}
\description{
Starts a Model Context Protocol (MCP) server that exposes FORCIS package
functions as tools for AI assistants such as Claude Desktop, Claude Code,
and VS Code GitHub Copilot.
}
\section{Configuration}{


\strong{Claude Desktop} — paste the following in your Claude Desktop configuration:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "forcis": \{
      "command": "Rscript",
      "args": ["-e", "forcis::forcis_mcp_server()"]
    \}
  \}
\}
}\if{html}{\out{</div>}}

\strong{Claude Code} — run in a terminal:

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{claude mcp add -s "user" forcis -- Rscript -e "forcis::forcis_mcp_server()"
}\if{html}{\out{</div>}}

\strong{VS Code} — add to \code{.vscode/mcp.json}:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "servers": \{
    "forcis": \{
      "command": "Rscript",
      "args": ["-e", "forcis::forcis_mcp_server()"]
    \}
  \}
\}
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Start the FORCIS MCP server (blocks the current process)
forcis_mcp_server()
}
}
